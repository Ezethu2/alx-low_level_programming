#include <stdio.h >
#include <ctype.h>

/**
  * main - prints alpha in lower case
  *
  * Return: Always 0
  */

int main(void)
{
int lowercase = 'a';
while (lower <= 'z')
{
putchar(lowercase);
lowercase += 1;
}
putchar('\n');
return (0);
{
