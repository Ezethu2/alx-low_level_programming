#include <stdio.h>
  2
    3 /**
	  4   * main - prints alpha in lower case
	    5   * Return: Always 0
	      6   */
      7 int main(void)
	  8 {
		    9
			     10         char lc;
		     11
			      12         for (lc = 'a'; lc <= 'z' lc++);
		      13         {
			       14                 putchar(lc);
			        15         }
		       16         putchar('\n');
		        17         return (0);
			 18 }#include <stdio.h>
			   2
			     3 /**
				   4   * main - prints alpha in lower case
				     5   * Return: Always 0
				       6   */
			       7 int main(void)
	  8 {
		    9
			     10         char lc;
		     11
			      12         for (lc = 'a'; lc <= 'z' lc++);
		      13         {
			       14                 putchar(lc);
			        15         }
		       16         putchar('\n');
		        17         return (0);
			 18 }

/**
  * main - prints alpha in lower case
  * Return: Always 0
  */
int main(void)
{

	char lc;

	for (lc = 'a'; lc <= 'z' lc++);
	{
		putchar(lc);
	}
	putchar('\n');
	return (0);
}
